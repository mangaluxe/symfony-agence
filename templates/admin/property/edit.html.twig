{% extends 'base.html.twig' %}

{% block title 'Gérer les biens' %}

{% block body %}

    <div class="container mt-4">
        <h1>Editer les biens</h1>

        {{ include ('admin/property/_form.html.twig', {button: 'Editer'}) }}
        
        {# Ci-dessous, c'est pareil dans edit.html.twig et new.html.twig, donc on met le tout dans un fichier pour faire include : #}
        {# {{ form_start(form) }}

        <div class="form">{{ form_row(form.title) }}</div>
        <div class="form">{{ form_row(form.surface) }}</div>
        <div class="form">{{ form_row(form.price) }}</div>
        <div class="form">{{ form_row(form.rooms) }}</div>
        <div class="form">{{ form_row(form.bedrooms) }}</div>
        <div class="form">{{ form_row(form.heat) }}</div>
        <div class="form">{{ form_row(form.floor) }}</div>
        <div class="form">{{ form_row(form.address) }}</div>
        <div class="form">{{ form_row(form.city) }}</div>
        <div class="form">{{ form_row(form.postal_code) }}</div>
        <div>{{ form_row(form.sold) }}</div>

        {{ form_widget(form) }}

        <button class="btn btn-primary">Save</button>

        {{ form_end(form) }} #}

    </div>



{% endblock %}